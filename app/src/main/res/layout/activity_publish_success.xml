<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Activities.Task.PublishSuccess">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_marginTop="110dp"
            android:id="@+id/tv_publish_success"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="您的任务已经成功发布!"
            android:textAlignment="center"
            android:textColor="@color/colorBlack"
            android:textSize="20dp" />

        <Button
            android:id="@+id/btn_publish_success_list"
            style="@style/ButtonWhiteRound"
            android:layout_height="40dp"
            android:layout_below="@+id/tv_publish_success"
            android:layout_marginTop="60dp"
            android:width="150dp"
            android:text="查看任务列表"/>

        <Button
            android:id="@+id/btn_publish_success_return"
            style="@style/ButtonGreenRound"
            android:layout_width="150dp"
            android:layout_height="40dp"
            android:layout_below="@id/btn_publish_success_list"
            android:layout_marginTop="30dp"
            android:text="返回" />
    </RelativeLayout>
</androidx.constraintlayout.widget.ConstraintLayout>