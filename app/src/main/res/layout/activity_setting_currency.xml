<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Activities.Setting.Setting_Currency">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar_setting_currency"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        app:layout_constraintTop_toTopOf="parent"
        app:popupTheme="@style/ThemeOverlay.AppCompat.Light" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_setting_currency">

        <TextView
            android:id="@+id/jiemiansetting"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableRight="@drawable/ic_into"
            android:padding="20dp"
            android:text="界面设置"
            android:textSize="20sp" />

        <View style="@style/Divider" />

        <TextView
            android:id="@+id/messagesetting"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:drawableRight="@drawable/ic_into"
            android:padding="20dp"
            android:text="消息设置"
            android:textSize="20sp" />

        <View style="@style/Divider" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>